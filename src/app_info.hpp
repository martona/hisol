#pragma once

#include <string_view>

namespace hisol {

inline constexpr std::string_view kName = "hisol";
inline constexpr std::string_view kExpansion = "HTTPS IPMI serial-over-LAN";
inline constexpr std::string_view kVersion = "0.1.0";

} // namespace hisol
